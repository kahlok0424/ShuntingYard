#include "Tokenizer.h"

/*Token tokenize(const char *expresson)
{
  Tokenizer *tokenizer = (Tokenizer *)0x0badface;
  Token *tokens = malloc(sizeof *tokens);
  const char *str = expresson;

  while(*str)
  {
    tokens = getToken(tokenizer);
  }
  return tokens;
}*/
